{% extends 'base.html.twig' %}

{% block title %}gerer d une adrresse{% endblock %}

{% block body %}

<h1>gerer une adresses</h1>
<a href="{{ path('account') }}">retourner a mon compte</a><br/>
<hr>
    {{ form_start(form) }}
    <div class="row">
    <div class="col-md-6">{{ form_row(form.name) }}</div>
        <div class="col-md-6">{{ form_row(form.adresse) }}</div>
    <div class="col-md-6">{{ form_row(form.phone) }}</div>
    <div class="col-md-6">{{ form_row(form.pays) }}</div>
    <div class="col-md-6">{{ form_row(form.city) }}</div>
    <div class="col-md-6">{{ form_row(form.postal) }}</div>
   </div>
    {{ form_end(form) }}
{% endblock %}